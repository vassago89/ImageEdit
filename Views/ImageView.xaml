<UserControl x:Class="ImageEdit.Views.ImageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ImageEdit.Views"
             xmlns:viewmodels="clr-namespace:ImageEdit.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewmodels:ImageViewModel/>
    </UserControl.DataContext>
    <Canvas>
        <Image Source="{Binding ImageStore.Source}"/>
        <Image Source="{Binding ImageStore.Background}"/>
        
    </Canvas>
</UserControl>
